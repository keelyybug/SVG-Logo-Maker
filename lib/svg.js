const fs = require('fs');
class SVG {
    constructor(){
        this.textElement = "",
        this.shapeElement =""
    }

    //look up how to writefile for SVG files

    //create a render method that returns a string that represents an SVG filewith our passed element

    //setText method that takes in the messge and a color and sets the text elementusing literals to pass in the message and color

    ////setShape() that takes in a shape and sets the shapeElement property to the shapes render method

}

module.exports = SVG;